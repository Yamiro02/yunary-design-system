import { BRAND_MONOGRAM, BRAND_NAME, BRAND_WORDMARK_LINES } from '@yunary/ds';

/* L'IDENTITÉ DU PROJET — pas celle du design system.
   `rebrand` met à jour src/brand.ts ; le prénom et le lieu d'exemple ci-dessous sont
   du CONTENU de vitrine, à éditer ici. */
export const IDENTITY = {
  wordmark: BRAND_WORDMARK_LINES,
  monogram: BRAND_MONOGRAM,
  prenom: 'Léa',
  personne: BRAND_NAME,
  lieu: 'Paris · France',
  titre: BRAND_NAME + ' — Design System',
};
